# Local development only. The Render build enables Google login automatically.
VITE_GOOGLE_LOGIN_ENABLED=true
# Leave unset to use the current browser origin. Vite proxies /api and /oauth2 locally.
# VITE_BACKEND_URL=http://localhost:8081
