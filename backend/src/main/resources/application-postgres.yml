spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:55432/spendwise}
    username: ${DB_USERNAME:spendwise}
    password: ${DB_PASSWORD:spendwise}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: false
